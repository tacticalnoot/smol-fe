---
import Layout from '../../layouts/Layout.astro';
import Glyph from '../../components/Glyph.svelte';

export const prerender = false;
---

<Layout>
	<Glyph id={Astro.params.id} client:only />
</Layout>
