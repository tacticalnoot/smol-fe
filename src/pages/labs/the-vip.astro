---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="THE VIP">
    <div class="container mx-auto px-4 py-12 min-h-screen">
        <header class="mb-12 text-center">
            <h1 class="text-4xl md:text-6xl font-heading font-black tracking-tighter text-transparent bg-clip-text bg-gradient-to-br from-white via-white to-white/50 mb-4">
                THE VIP
            </h1>
            <p class="text-white/60 max-w-xl mx-auto font-light">
                Secure, end-to-end encrypted communications for the Smol ecosystem.
                <br/>
                <span class="text-xs text-white/30 mt-2 block font-mono">
                    NO FEES • NO LOGS • NO TRACES
                </span>
            </p>
        </header>

        <section class="max-w-4xl mx-auto">
             <!-- Svelte Component Mount -->
             <!-- We need a Client-side wrapper/controller because this is heavily interactive. -->
             <div id="vip-root"></div>
        </section>
        
        <!-- Load the client-side app -->
        <script>
            import ChatApp from '../../components/chat/ChatApp.svelte';
            import { mount } from 'svelte';
            
            // Astro 5 / Svelte 5 mounting
            // Or usual island hydration if we used <ChatApp client:only />
            // Using explicit script gives us more control over the initial load state if needed,
            // but <ChatApp client:only="svelte" /> is cleaner in Astro.
        </script>
    </div>
</Layout>

<script>
    // Just redirection logic or simple setup if needed. 
    // Actually, I should use the simplified component in the template above.
</script>

<!-- Using Astro Island for the App -->
<style>
    :global(body) {
        background-color: #050505;
    }
</style>
