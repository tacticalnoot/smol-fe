import { contract, scValToNative } from "@stellar/stellar-sdk/minimal";
console.log("contract.scValToNative:", typeof contract.scValToNative);
console.log("scValToNative at root:", typeof scValToNative);
