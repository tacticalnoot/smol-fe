# AI Agent Quickstart

**Canonical Context for Agents**

## 🛑 Prime Directives
1.  **Do Not Break Auth**: Passkey auth is delicate. Understand `src/utils/passkey-kit.ts` before touching.
2.  **Svelte 5 Runes**: We use `$state`, `$derived`, `$effect`. Do NOT use Svelte 4 `writable` stores.
3.  **Tailwind 4**: Use utility classes. Avoid `<style>` blocks.
4.  **No "npm"**: Use `pnpm` exclusively.

## 🗺️ Navigation
-   **Map**: `docs/REPO_MAP.json` (Machine readable structure)
-   **Docs**: `docs/INDEX.md` (Human readable index)
-   **API**: `docs/INTEGRATION_REFERENCE.md` (Backend contract)

## ⚡ Common Task Flows

### 1. Adding a New Page
1.  Create `src/pages/my-page.astro`.
2.  Use MainLayout: `<Layout title="...">`.
3.  Add Svelte components in `src/components/`.

### 2. Modifying State
1.  Open `src/stores/`.
2.  Identify the rune-based store (e.g., `user.svelte.ts`).
3.  Update the class method or export.

### 3. Debugging
1.  Check `docs/DEBUGGING_GUIDE.md`.
2.  Use `smolLogger` in browser console.

## ⚠️ Danger Zones
-   **`src/utils/passkey-kit.ts`**: WebAuthn wrapper. High regression risk.
-   **`ext/`**: Autogenerated code. Do not edit. Regenerate using scripts if needed.
