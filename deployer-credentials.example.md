# Deployer Wallet Credentials (Example)

Use this as a local template only.

- Copy to `deployer-credentials.md` locally when needed.
- Never commit real private keys or seed phrases.
- Keep the actual secret in `deployer_secret.txt` locally; that file is gitignored.

Public Key: `G...`
Secret Key: `S...`
